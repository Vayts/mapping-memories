import styled from 'styled-components';

export const Container = styled.div`
  max-width: 1220px;
  margin: 60px auto 0;
  padding: 0 10px 40px;
  position: relative;
  min-height: calc(100vh - 60px);
`;
